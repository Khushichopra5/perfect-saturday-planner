# Optional: enable the LLM narrator (OpenAI-compatible API).
# Without these the agent still works fully using its deterministic planner.
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
